@using EntityLayer.Concreate
@model List<WriterMessage>
@{
    ViewData["Title"] = "SenderMessage";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Giden Kutusu</h4>
            <p class="card-description">

            </p>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Konu</th>
                            <th>Alıcı</th>
                            <th>Tarih</th>

                            <th>Detaylar</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {


                            <tr>
                                <td>@item.WriterMessageid</td>
                                <td>@item.Subject</td>
                                <td>@item.ReciverName</td>
                                <td>@(((DateTime)item.Date).ToString("dd-MM-yyyy"))</td>

                                <td><label class=""></label></td>
                                <td>
                                    <a href="/Writer/Message/ReciverMessagDetails/@item.WriterMessageid" class="btn btn-outline-primary">Mesajı Gör</a>
                                </td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>


